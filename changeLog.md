# changeLog — 나만의정보

> 완료 기록(역순). 앞으로 할 일은 [plan.md](plan.md), 함정은 [claude-docs/troubleshooting.md](claude-docs/troubleshooting.md).
> 코드 세부는 PR·커밋에 있다 — 여기엔 **왜 / 무엇을**만 적는다.

---

## 2026-08-24 · 배포 준비 — `app` 함수 JWT 검증 해제

**의도**: 앱인토스 승인이 나면서 클라우드 배포 경로가 열렸다. 배포하자마자 터질 것을 먼저 막는다.

**결과**:

- **`app` 함수만 게이트웨이 JWT 검증을 끈다.** 프런트는 Supabase JWT 가 아니라 자체 HMAC 세션 토큰을 `Authorization: Bearer` 로 보내고, `/login` 은 아예 인증 헤더가 없다. `config.toml` 에 `[functions]` 절이 아예 없어 전 함수가 기본값 `verify_jwt = true` 였으므로, 그대로 배포했으면 게이트웨이가 우리 코드에 닿기 전에 401 을 냈다. 인증은 함수가 직접 한다 — `verifySession` / 토스 코드 교환 / unlink 공유 시크릿. 나머지 4개는 크론이 `service_role` Bearer 로 부르므로 검증을 켜 둔 채로 둔다.
- **배포 절차를 plan 에 체크박스로 박았다.** 대시보드에서 프로젝트를 만드는 순간부터 수동 스모크까지. 함정 셋을 같이 적었다 — 확장이 `cron`/`net` 이 아닌 스키마에 깔리면 `invoke_job` 이 조용히 죽고, `db push` 는 크론 4개를 즉시 무장시키며, 무료 티어의 7일 비활성 정지가 이 앱처럼 외부 요청이 0인 구조에 걸리는지는 아직 모른다.
- **푸시를 인증서보다 앞에 뒀다.** 문구 사전 검수는 리드타임이 미지수인데 남의 시간이라 임계 경로다. 인증서·배포는 우리가 통제하는 시간이니 뒤로 갈 수 있다.
- **아직 못 한 것**: `app/.env.production` 이 없어 prod 빌드의 `VITE_API_BASE` 가 `undefined` 다. project-ref 가 나와야 채운다.

---
## 2026-08-23 · UI 완성도 — 기사 썸네일·아이콘·주제 배지

**의도**: 직전 작업이 TDS를 입혔지만 결과가 "미니멀"이 아니라 "밋밋"했다 — 이미지 0장, 아이콘 0개, OS 이모지(🥛🌏✅) 톤 불일치, 기사가 왜 골라졌는지 화면에 없음. 장식이 아니라 **정보 전달에 기여하는 시각 요소**를 되찾는다. 설계: [2026-08-23-ui-polish-design.md](docs/superpowers/specs/2026-08-23-ui-polish-design.md).

**결과**:

- **썸네일은 두 겹으로 모은다.** 먼저 RSS에서 공짜로 얻고(`media:thumbnail` → 이미지 타입 `media:content` → 본문 첫 `<img>`), 그래도 없는 신규 기사만 collect 잡이 원문을 받아 `og:image`로 메운다. 라이브 실측 커버리지: BBC·Ars·ZDNet은 RSS만으로 100%, 연합뉴스 89/120, 한국경제는 RSS 0/50이지만 og 백필이 채운다. 이미지 프록시·리사이즈 서버는 안 만들었다 — 원본 CDN URL 직결이고 실패하면 프런트가 영역을 접는다.
- **언론사 로고 필터가 이 기능의 성패였다.** 표본 10건의 og:image 중 **5건이 기사 사진이 아니라 언론사 로고**였다(연합뉴스 3/5, 한국경제 2/5). 걸러내지 않으면 브리핑 카드 절반에 똑같은 로고가 붙어 없느니만 못한 화면이 된다. URL에 `logo`가 있으면 버린다 — 오탐해도 결과는 "이미지 없음"이라 최악이 현상 유지다. 실제 기사 사진(`/photo/…`)이 통과하는지도 반대 방향으로 테스트했고, 라이브 기사로 양방향 확인했다.
- **og 백필은 collect 안에 뒀다.** 전용 Function + 크론으로 분리하면 배포 대상이 늘어난다. 하루 1회 잡이라 상한 120건·시간 예산 60초·동시 6으로 바운드하고, 남는 건 null로 둔다. 커버리지가 모자라면 그때 분리하는 승격 경로를 주석에 남겼다.
- **레이아웃이 "이미지 혼재"를 흡수한다.** 카드 상단 히어로가 아니라 **제목 오른쪽 72×72 소형 썸네일**이라, 이미지가 없는 카드는 "빈 자리"가 아니라 "제목이 전체 폭을 쓰는 행"으로 보인다. 목 데이터를 일부러 4건 채움 + 1건 없음으로 만들어 스크린샷 자체가 이걸 증명하게 했다.
- **아이콘 9자리 도입, OS 이모지 전량 제거.** 인트로 가치 3줄은 색 있는 `ListRow.AssetIcon`, 설정 진입은 `IconButton`, 배너·빈 상태·끝 마감은 `Asset.Icon`. 아이콘 `name`이 `string` 타입이라 타입 검사가 오타를 못 잡으므로 쓴 이름 8개를 전부 CDN HEAD로 확인했다(전부 200).
- **주제 배지**로 "이 기사가 왜 왔는지"를 카드에 노출했다(`articles.topics` → API → `Badge`). 카드 메타에는 상대 발행 시각("16시간 전")을 붙였다 — 후보 창이 3일이라 날짜 없는 절대 시각은 오해를 만든다.
- **대체 시각 요소는 만들지 않았다.** 이미지 없는 기사에 주제색 블록이나 이니셜 아바타를 깔면 절반이 똑같은 색 블록이 돼 오히려 더 성의 없어 보인다. 정보 기여가 0인 장식은 안 넣는다는 것이 이번 작업의 기준이다.

**테스트 71 → 91건** (rss 이미지 추출 7 + og 추출 13). 새 규칙마다 돌연변이로 사살을 확인했다 — 특히 로고 필터를 제거하면 로고 테스트 3건이, 과하게 넓히면 통과 테스트까지 10건이 깨진다. 심사 스크린샷 3장은 새 화면으로 다시 찍었다(636×1048 실측).

**미검증**: 앱인토스 웹뷰가 외부 이미지 도메인을 허용하는지(설정으로 제어 불가 — 차단되면 썸네일이 전부 접히고 이전 상태로 돌아간다), `navigationBar` 명시 설정의 실기기 동작, 배포 후 `ogFetched` 실측 커버리지.

---

## 2026-08-23 · 화면 디자인(TDS) + 심사 스크린샷 경로

**의도**: 화면 4개가 브라우저 기본 태그(h1·button·select)로만 그려져 있었다. TDS 사용 자체가 앱인토스 심사 기준이고, 심사 제출에는 세로 스크린샷 3장(636×1048)이 필요하다. 그런데 mTLS 인증서가 아직 없어 `POST /app/login`이 성공한 적이 없다 — 로그인이 필요한 브리핑·설정은 띄울 방법 자체가 없었다.

**결과**: 목 경로를 먼저 깔고 그 위에서 디자인을 입혔다.

- **목 경로 = `vite dev --mode mock` + `api.ts` 게이트**. 셸 환경변수(`VITE_MOCK=1 npm run dev`)는 PowerShell에서 안 먹고 cross-env는 새 의존성이라 vite mode로 걸었다. `api.ts`의 7개 export를 삼항으로 감싸 목/실 구현을 고른다 — 화면 코드는 한 줄도 목을 모른다. 쿼리스트링(`?screen=intro`·`?screen=onboarding`)으로 시작 화면을 고르므로 App.tsx도 안 고쳤다. alias 스왑은 조용히 안 걸리면 실 API를 목인 줄 알고 쓰게 되므로 기각했다.
- **prod 오염은 grep으로 확정했다**. 목 기사 문자열이 소스에 1건 잡히는 것을 먼저 확인하고(양성 대조), 같은 문자열로 `dist/`를 훑어 0건임을 봤다. Vite가 `import.meta.env`를 정적 치환해 분기가 접히고 `mock.ts`가 트리셰이킹된다는 가정이 실측으로 확정됐다.
- **디자인은 TDS 컴포넌트만 쓴다** — 표에 올린 컴포넌트·prop을 전부 `@toss/tds-mobile`의 d.ts에서 존재와 시그니처를 확인하고 썼다. 색은 hex 대신 `adaptive.*`(CSS 변수)라 다크모드가 공짜다. TDS에 카드 컴포넌트가 없어 토스 문법대로 평면 + 구분선(`Border`)으로 갔다.
- **브랜드 색을 로고 파랑 `#3E7BD1`로 바꿨다**. 기존 `#3182F6`은 토스 기본값 그대로 = 브랜드 색을 안 정했다는 신호다. 이 값 하나가 Provider를 타고 CTA·버튼 주색이 된다.
- **설정 화면의 자체 「뒤로」 버튼을 지웠다** — 토스가 네이티브 내비게이션 바를 제공한다는 가정이다(실기기 미검증, 틀리면 1줄 복원). `<select>`는 `BottomSheet.Select` 24개 라디오로, 체크박스는 `Switch`로, 인라인 에러 문구는 `useToast`로 갈아탔다.
- **스크린샷 3장**: 논리 318×524 + `deviceScaleFactor 2`로 캡처해 정확히 636×1048이 나왔다(`System.Drawing`으로 실측). `store-assets/screenshots/`.

**테스트는 안 늘렸다** — `app/`에는 러너가 없고 새 의존성이 금지다. 검증 게이트는 ① `tsc -b && vite build && ait build` ② 목 모드 브라우저 실측(각 태스크마다) ③ prod 번들 grep이다. 백엔드 테스트 71개는 그대로 통과한다(프런트만 건드렸다).

**여전히 미검증**: 네이티브 내비 바의 뒤로가기 존재, 실기기에서의 이모지 렌더(Tossface), 실제 토스 로그인. 전부 콘솔 등록·샌드박스가 선행돼야 한다.

---

## 2026-08-23 · 프런트엔드 — 미니앱 화면 3개 + 앱 전용 API

**의도**: 백엔드가 쌓아 둔 브리핑을 사람이 볼 수 있게 만든다. RLS를 전면 차단해 둬서 앱이 DB에 직접 붙는 경로가 없으므로, 앱이 부를 Edge Function `app` 하나를 같이 만든다.

**결과**: 화면 3개(인트로+온보딩 / 오늘의 브리핑 / 설정)와 Edge Function `app`(경로 라우팅 4개 + unlink 콜백)이 붙었다. 세션은 무상태 HMAC 토큰이라 DB 저장이 0이고 JWT 라이브러리도 안 들였다 — 담을 클레임이 `userKey`와 만료 둘뿐이다. 상태관리 라이브러리·라우터·react-query도 안 들였다(화면 3개는 `useState` + `fetch` + history API로 끝난다). 테스트 71개 통과(기존 41 + 세션 8 + 복호화 11 + 라우팅 11).

**고정 어휘를 프런트가 서버 파일에서 직접 import한다** — 온보딩 선택지와 서버 검증이 같은 `_shared/topics.ts`를 본다. 한글 라벨만 프런트 전용이다. 프런트를 같은 레포에 둔 이유가 이것이고, Vite가 프로젝트 루트 밖 파일을 못 읽어 `@shared` alias와 `server.fs.allow`를 같이 열어야 했다.

**계획서에 없던 판단 넷을 구현 중에 넣었다**:

- **프런트 빌드에 타입 검사를 게이트로 걸었다** — 스캐폴딩 기본 `build`에 `tsc`가 없었다. TypeScript를 쓰면서 검사를 안 부르면 타입이 장식이 된다. `build`를 `tsc -b && vite build && ait build`로 바꾸고 `tsconfig.app.json`에 `@shared/*` paths를 등록했다.
- **`POST /app/unlink`를 fail-closed로 닫았다** — 설계 초안은 "콜백 인증 방식이 미확인이니 로깅+존재 검증만"이라고 적었는데, 그대로 배포하면 `userKey` 하나만 알면 누구나 남의 데이터를 지울 수 있는 공개 삭제 경로가 된다. `UNLINK_CALLBACK_SECRET` 미설정이면 503, 있으면 `x-unlink-secret` 헤더를 대조해 불일치는 401. 콘솔에서 실제 인증 방식을 확인하면 교체한다.
- **deprecated SDK 함수를 현행 API로 교체** — `appLogin()` → `TossAuth.login()`, `openURL(url)` → `Device.openURL(url)`. 둘 다 SDK 3.0.2 타입 정의에 deprecated로 표시돼 있고 시그니처가 같다.
- **`app/src/App.css` 삭제** — 새 `App.tsx`가 import하지 않는 스캐폴딩 잔재였다.

**설계의 미확인 하나가 코드를 읽어서 해소됐다.** `openURL`이 외부 브라우저인지 인앱 웹뷰인지가 미확인이었는데, SDK 타입 정의가 「지정한 URL을 기기의 기본 브라우저나 관련 앱에서 열어요」라고 명시한다 — 저작권 방침(원문을 재배포하지 않고 외부 브라우저로 넘긴다)의 전제가 문서로 확인됐다. 다만 뉴스 링크가 `openURL` 운영 정책상 허용 대상인지는 여전히 미확인이라 심사 제출 전에 정책 문서로 확정해야 한다.

**미검증으로 남은 것** — 전부 토스 콘솔 등록이나 키 수령이 선행돼야 한다:

- **실제 토스 로그인**: 일반 브라우저에는 토스 SDK가 없어 `TossAuth.login()`이 실패한다. 로컬에서 확인한 것은 인트로가 뜨고 「시작하기」가 오류 메시지를 내는 데까지다. 로그인 교환·세션 발급 경로는 코드만 있고 한 번도 안 돌았다.
- **실기기(샌드박스)**: 내비게이션 바 뒤로가기 ↔ WebView history 연동, 웰컴 브리핑, `Device.openURL` 실동작, 재진입 시 세션 유지.
- **복호화 배치 가정**: 토스가 GCM 태그를 암호문 뒤에 붙이는 표준 배치를 쓰는지, `birthday`가 `YYYYMMDD`인지 미확인이다. 테스트는 `node:crypto`로 만든 픽스처를 왕복시켜 **우리 구현이 표준 배치에 맞다**는 것만 증명한다 — 토스가 다른 배치를 쓰면 통과하는 테스트를 두고도 실데이터에서 실패한다. 키 수령 후 실데이터 1건으로 확정한다.

서버 쪽은 로컬 `supabase functions serve`로 401(세션 없음) / 404(모르는 경로) / 503(unlink 미설정) / 400(어휘 밖 PUT) / 200(세션 붙인 브리핑 조회 — 카드 5건 실제 반환)을 전부 확인했다.

## 2026-08-23 · 프런트 설계 + mTLS 스파이크 + 구현 계획

**의도**: Phase 6(미니앱 프런트)에 들어가기 전에 앱-서버 인증 구조를 정하고, 최대 리스크였던 mTLS 가부를 실측으로 걷어낸다.

**결과**: 설계에서 판단 셋이 굳었다. 토스 refreshToken을 아예 보관하지 않고(로그인 이후 쓸 데가 없는데 보관하면 암호화 저장 의무만 생긴다) 무상태 HMAC 세션으로 간다. 앱 API는 Edge Function 하나에 경로 라우팅 4개. 온보딩 완료 순간 웰컴 브리핑을 만들어 가입 직후 빈 화면을 없앤다 — 심사자가 핵심 기능을 못 보는 리스크를 직접 줄인다.

**mTLS 스파이크**: 토스 파트너 API가 전부 mTLS인데 Supabase Edge Functions에서 되는지가 미확인이었다. 토스 인증서 없이 badssl 공개 테스트 인증서로 실측해 로컬 edge-runtime 1.74.3에서 통과를 확인했다(200/400 대조). 프록시 분리가 불필요해져 인프라가 하나도 안 늘었다. 계측기는 세 겹으로 통제했다 — 인증서 만료일 확인, Node 대조군, 음성 대조. 셋 중 하나라도 없으면 실패했을 때 원인을 못 가른다.

**계획 작성 중 실측으로 드러난 것**: `create-ait-app`은 `--tds --inline` 없이는 React도 TDS도 없는 바닐라 TS 템플릿을 만들고 대화형 프롬프트에서 멈춘다. 설정 파일 이름도 설계가 적은 `granite.config.ts`가 아니라 `apps-in-toss.config.ts`다.

## 2026-08-22 · 요약 정확도 가드 + `sports` 어휘 추가

**의도**: 파이프라인은 정상인데 **내용이 틀렸다.** 정치 기사 요약이 발언 주체를 여↔야로 뒤집은 채로 테스트 37개를 전부 통과했다 — 형식 계측기만 있고 내용을 보는 계측기는 없었다. 동시에 스포츠 기사가 갈 칸이 없어 `culture`로 밀려 브리핑 6칸 중 2칸을 차지했다.

**결과**: 모델을 올리지 않고 **프롬프트 가드를 먼저** 넣었다(Haiku 4.5 유지) — 발언 주체·찬반·인과는 원문에 명시된 대로만 쓰고, 주체 표기(「與」 등)를 해석해 바꾸지 않는다. 고정 어휘에 `sports`를 추가했다(12개 → 13개). 단 `profile_rules` 시드에는 넣지 않는다 — 호불호가 갈려 나이·직업으로 미는 것이 맞지 않고 온보딩에서 직접 고른 사람에게만 가야 한다.

**같은 기사 3건을 실제로 다시 요약해 판정이 뒤집힌 것을 확인했다 — 3건 모두 통과.** 기사 133은 짝이 바로 잡혔고(`與`=재정 안정 / `국힘`=꼼수), 가드가 요구한 대로 **원문 표기를 그대로 유지**해 변환 자체를 하지 않았다. 131·132는 `sports` 태그를 받았다. 비용은 미요약 319건을 임시 마커로 막아 3건으로 좁혀서 통제했고, 검증 후 마커를 0건으로 되돌렸다.

테스트는 37개 그대로다. `sports`가 어휘에 들어오면서 그것을 「어휘 밖 태그」 예시로 쓰던 테스트 2건이 깨졌는데 — 깨진 것 자체가 그 테스트가 어휘 필터를 실제로 겨누고 있다는 증거라 검증 의도는 두고 예시 태그만 바꿨다.

함정은 [T-004](claude-docs/troubleshooting/T-004.md)에 남겼다 — **LLM 출력물은 형식 테스트가 통과해도 내용이 틀릴 수 있다는 것을 전제로 다룬다.**

## 2026-08-22 · 백엔드 파이프라인 (수집·요약·배달)

**의도**: 브리핑이 매일 자동으로 쌓이는 상태를 만든다. 프런트가 붙기 전에 데이터가 먼저 있어야 한다.

**결과**: Edge Function 4개(수집 / 요약 제출 / 요약 수거 / 배달)와 pg_cron 스케줄 4건을 붙였다. 순수 로직(KST 변환·RSS 파싱·요약 응답 파싱·점수 계산)은 런타임 중립 모듈로 떼어 `node --test`로 검증했다 — 테스트 프레임워크를 하나도 추가하지 않았다. 테스트 37개 통과.

**실측으로 한 바퀴 돌렸다.** 소스 9개에서 기사 324건을 수집했고, 실제 API 키로 5건을 Batch API에 보내 한국어 요약과 고정 어휘 태그가 DB에 들어오는 것까지 확인했다. 영문 기사(BBC World)도 한국어로 요약됐다. 테스트 프로필로 `deliver`를 돌려 `briefings` 5건이 확정됐고, 해외 1건 보장이 실제로 걸렸다. 비용 통제는 코드를 고치지 않고 DB에서 미요약 대상을 5건으로 좁혀서 했다 — 임시 마커는 검증 후 0건으로 되돌렸다.

스펙에서 바꾼 것 다섯: 요약 잡 2단계 분할(Batch는 비동기인데 Edge Function엔 실행 시간 제한이 있다), 점수 계산을 SQL에서 TS로(다양성·해외 보장 규칙을 pgTAP 대신 순수 함수 테스트로), RLS 전면 차단(토스 로그인이 Supabase Auth가 아니라 `userKey`를 신뢰할 수 없다), 프롬프트 캐싱 제거(시스템 프롬프트가 최소 캐시 프리픽스에 못 미쳐 조용히 캐시되지 않는다), 요약 잡 테스트를 LLM 목킹 대신 응답 파서 단위 테스트 + 실제 한 바퀴 실행으로.

구현하며 계획서 자체의 결함 넷이 드러났다:

- **해외 1건 보장이 자리가 남아도 최하위를 밀어냈다.** 계획서 의사코드가 "교체"만 상정했는데, 정원이 안 찼으면 교체가 아니라 추가여야 한다. 두 분기로 갈랐다.
- **`deno.json` 위치.** 함수 디렉터리마다가 아니라 `supabase/functions/` 한 곳에 둬야 import map 이 전 함수에 걸린다.
- **`service_role` GRANT 누락.** RLS 차단만 넣었더니 파이프라인이 `permission denied (42501)`로 전부 죽었다 — 최근 Supabase CLI가 신규 테이블을 Data API 롤에 자동 노출하지 않는다.
- **피드 검증기가 실제 파서를 안 태웠다.** 원시 `<item>` 태그 개수로 판정해 파싱 0건인 소스를 통과시켰다([T-003](claude-docs/troubleshooting/T-003.md)). `parseFeed` 반환 건수로 바꾸니 판정이 뒤집혀 한겨레를 뺐다.

## 2026-08-22 · 백엔드 파이프라인 구현 계획 작성

**의도**: 구현에 들어가기 전에 태스크를 스텝 단위로 쪼개 둔다. 실측 없이 쓴 계획은 명령어가 헛돈다.

**결과**: 태스크 14개짜리 TDD 계획을 [docs/superpowers/plans/2026-08-22-backend-pipeline.md](docs/superpowers/plans/2026-08-22-backend-pipeline.md)에 썼다. 범위는 수집·요약·배달까지 — 프런트와 푸시는 별도 계획으로 뗐다.

환경을 먼저 실측해 스택을 정했다. Node 24가 `.ts` 테스트를 그대로 돌려서 **테스트 프레임워크를 하나도 추가하지 않는다**(vitest·jest·ts-node 불필요). supabase CLI는 npx로 쓴다. `fast-xml-parser`의 실제 동작(빈 문자열·깨진 XML의 반환값, 단일 항목이 배열이 아닌 것, HTML 엔티티 디코드)도 돌려 보고 테스트 기대값에 반영했다.

구현 가능성을 따지며 스펙에서 바꿀 것 5건을 계획에 명시했다 — 요약 잡 2단계 분할, 점수 계산을 SQL에서 TS로, RLS 전면 차단, 프롬프트 캐싱 제거, 요약 잡 테스트 범위 축소. 스펙 문서 반영은 구현이 끝나는 시점(Task 14)에 한다.

## 2026-08-22 · 프로젝트 셋업 (작업 추적 3종 + 원격 레포)

**의도**: 첫 구현에 들어가기 전에 운영 로그 뼈대를 세운다. 판단을 미루면 판단 자체를 잊는다.

**결과**: `plan.md` / `changeLog.md` / `claude-docs/troubleshooting/` 분할 시스템(자동 목차 + 검사기 + pre-commit 훅)을 배치하고 `.gitattributes`·`.gitignore`를 맞췄다. 첫 함정 항목 [T-001](claude-docs/troubleshooting/T-001.md)을 기록했다 — GitHub 레포명의 한글이 에러 없이 하이픈으로 치환되는 무성 실패.

## 2026-08-22 · 개인 맞춤 아침 브리핑 MVP 설계 확정

**의도**: 정보 과잉 속에서 개인 환경(나이·성별·가구·직업)에 맞는 정보를 골라 배달한다. 특히 접하기 번거로운 해외 매체를 한글 요약으로 같이 준다.

**결과**: 앱인토스 플랫폼 제약을 실측으로 확인하고(WebView 스택, 로그인 스코프에 성별·생일 포함, 푸시는 사전 검수 + 예약 발송 없음) MVP 설계를 확정했다. 핵심 결정 셋:

- **아침 배달 모델** — 무한 스크롤이 아니라 하루 한 번 끝나는 묶음. 정보 과잉이 문제인데 피드를 또 만들면 문제를 재현한다.
- **요약은 기사당 1회, 전 사용자 공유** — AI 비용이 사용자 수와 무관하고 수집 기사 수에만 비례한다. 개인화는 SQL 점수 계산으로 따로 한다.
- **원문 재배포 없음** — 요약 + 출처 + 원문 링크만. 본문 크롤링을 하지 않는 것이 저작권 방어선이다.

요약 모델은 Haiku 4.5 + Batch API(50% 할인)로 월 2~3만 원 수준을 잡았다.
