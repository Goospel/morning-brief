# changeLog — 나만의정보

> 완료 기록(역순). 앞으로 할 일은 [plan.md](plan.md), 함정은 [claude-docs/troubleshooting.md](claude-docs/troubleshooting.md).
> 코드 세부는 PR·커밋에 있다 — 여기엔 **왜 / 무엇을**만 적는다.

---

## 2026-08-22 · 백엔드 파이프라인 (수집·요약·배달)

**의도**: 브리핑이 매일 자동으로 쌓이는 상태를 만든다. 프런트가 붙기 전에 데이터가 먼저 있어야 한다.

**결과**: Edge Function 4개(수집 / 요약 제출 / 요약 수거 / 배달)와 pg_cron 스케줄 4건을 붙였다. 순수 로직(KST 변환·RSS 파싱·요약 응답 파싱·점수 계산)은 런타임 중립 모듈로 떼어 `node --test`로 검증했다 — 테스트 프레임워크를 하나도 추가하지 않았다. 테스트 37개 통과.

**실측으로 한 바퀴 돌렸다.** 소스 9개에서 기사 324건을 수집했고, 실제 API 키로 5건을 Batch API에 보내 한국어 요약과 고정 어휘 태그가 DB에 들어오는 것까지 확인했다. 영문 기사(BBC World)도 한국어로 요약됐다. 테스트 프로필로 `deliver`를 돌려 `briefings` 5건이 확정됐고, 해외 1건 보장이 실제로 걸렸다. 비용 통제는 코드를 고치지 않고 DB에서 미요약 대상을 5건으로 좁혀서 했다 — 임시 마커는 검증 후 0건으로 되돌렸다.

스펙에서 바꾼 것 다섯: 요약 잡 2단계 분할(Batch는 비동기인데 Edge Function엔 실행 시간 제한이 있다), 점수 계산을 SQL에서 TS로(다양성·해외 보장 규칙을 pgTAP 대신 순수 함수 테스트로), RLS 전면 차단(토스 로그인이 Supabase Auth가 아니라 `userKey`를 신뢰할 수 없다), 프롬프트 캐싱 제거(시스템 프롬프트가 최소 캐시 프리픽스에 못 미쳐 조용히 캐시되지 않는다), 요약 잡 테스트를 LLM 목킹 대신 응답 파서 단위 테스트 + 실제 한 바퀴 실행으로.

구현하며 계획서 자체의 결함 넷이 드러났다:

- **해외 1건 보장이 자리가 남아도 최하위를 밀어냈다.** 계획서 의사코드가 "교체"만 상정했는데, 정원이 안 찼으면 교체가 아니라 추가여야 한다. 두 분기로 갈랐다.
- **`deno.json` 위치.** 함수 디렉터리마다가 아니라 `supabase/functions/` 한 곳에 둬야 import map 이 전 함수에 걸린다.
- **`service_role` GRANT 누락.** RLS 차단만 넣었더니 파이프라인이 `permission denied (42501)`로 전부 죽었다 — 최근 Supabase CLI가 신규 테이블을 Data API 롤에 자동 노출하지 않는다.
- **피드 검증기가 실제 파서를 안 태웠다.** 원시 `<item>` 태그 개수로 판정해 파싱 0건인 소스를 통과시켰다([T-003](claude-docs/troubleshooting/T-003.md)). `parseFeed` 반환 건수로 바꾸니 판정이 뒤집혀 한겨레를 뺐다.

## 2026-08-22 · 백엔드 파이프라인 구현 계획 작성

**의도**: 구현에 들어가기 전에 태스크를 스텝 단위로 쪼개 둔다. 실측 없이 쓴 계획은 명령어가 헛돈다.

**결과**: 태스크 14개짜리 TDD 계획을 [docs/superpowers/plans/2026-08-22-backend-pipeline.md](docs/superpowers/plans/2026-08-22-backend-pipeline.md)에 썼다. 범위는 수집·요약·배달까지 — 프런트와 푸시는 별도 계획으로 뗐다.

환경을 먼저 실측해 스택을 정했다. Node 24가 `.ts` 테스트를 그대로 돌려서 **테스트 프레임워크를 하나도 추가하지 않는다**(vitest·jest·ts-node 불필요). supabase CLI는 npx로 쓴다. `fast-xml-parser`의 실제 동작(빈 문자열·깨진 XML의 반환값, 단일 항목이 배열이 아닌 것, HTML 엔티티 디코드)도 돌려 보고 테스트 기대값에 반영했다.

구현 가능성을 따지며 스펙에서 바꿀 것 5건을 계획에 명시했다 — 요약 잡 2단계 분할, 점수 계산을 SQL에서 TS로, RLS 전면 차단, 프롬프트 캐싱 제거, 요약 잡 테스트 범위 축소. 스펙 문서 반영은 구현이 끝나는 시점(Task 14)에 한다.

## 2026-08-22 · 프로젝트 셋업 (작업 추적 3종 + 원격 레포)

**의도**: 첫 구현에 들어가기 전에 운영 로그 뼈대를 세운다. 판단을 미루면 판단 자체를 잊는다.

**결과**: `plan.md` / `changeLog.md` / `claude-docs/troubleshooting/` 분할 시스템(자동 목차 + 검사기 + pre-commit 훅)을 배치하고 `.gitattributes`·`.gitignore`를 맞췄다. 첫 함정 항목 [T-001](claude-docs/troubleshooting/T-001.md)을 기록했다 — GitHub 레포명의 한글이 에러 없이 하이픈으로 치환되는 무성 실패.

## 2026-08-22 · 개인 맞춤 아침 브리핑 MVP 설계 확정

**의도**: 정보 과잉 속에서 개인 환경(나이·성별·가구·직업)에 맞는 정보를 골라 배달한다. 특히 접하기 번거로운 해외 매체를 한글 요약으로 같이 준다.

**결과**: 앱인토스 플랫폼 제약을 실측으로 확인하고(WebView 스택, 로그인 스코프에 성별·생일 포함, 푸시는 사전 검수 + 예약 발송 없음) MVP 설계를 확정했다. 핵심 결정 셋:

- **아침 배달 모델** — 무한 스크롤이 아니라 하루 한 번 끝나는 묶음. 정보 과잉이 문제인데 피드를 또 만들면 문제를 재현한다.
- **요약은 기사당 1회, 전 사용자 공유** — AI 비용이 사용자 수와 무관하고 수집 기사 수에만 비례한다. 개인화는 SQL 점수 계산으로 따로 한다.
- **원문 재배포 없음** — 요약 + 출처 + 원문 링크만. 본문 크롤링을 하지 않는 것이 저작권 방어선이다.

요약 모델은 Haiku 4.5 + Batch API(50% 할인)로 월 2~3만 원 수준을 잡았다.
